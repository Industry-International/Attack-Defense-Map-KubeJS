# KubeJS 持久化数据存储说明

> 适用环境：NeoForge 1.21.1 · KubeJS 7

## 一、存储机制概述

本项目使用 `server.persistentData` 进行持久化数据存储。

### 存储位置

```
saves/<世界名>/kubejs/persistent_data.nbt
```

### 与 Minecraft 原版存储的区别

| 特性 | `server.persistentData`（本项目使用） | Minecraft `/data merge storage` |
|------|--------------------------------------|-------------------------------|
| 存储位置 | `saves/<世界>/kubejs/` 目录下 | `level.dat` 内的 `minecraft:persistent` |
| 归属方 | **KubeJS 独立存储** | Minecraft 原版命令存储 |
| 对数据包的影响 | **无影响** | 数据包常用此系统 |
| API 访问方式 | `server.persistentData.putString/getString` | `/data merge storage` 命令 |
| 读写方式 | JSON 字符串读写 | NBT 直接操作 |

**结论：`server.persistentData` 不会污染 Minecraft 原版数据存储系统，不会干扰数据包开发。**

## 二、模块存储结构

### 2.1 SBW 载具系统

**存储键：** `sbw_vehicle`
**代码位置：** `server_scripts/sbw_vehicle/main.js`

```json
{
  "active": true,
  "vehicles": {
    "attack_tank_1": {
      "status": "alive",
      "team": "attack",
      "vehicleType": "superbwarfare:t_90a",
      "uuid": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
    },
    "defense_tank_1": {
      "status": "respawning",
      "team": "defense",
      "vehicleType": "superbwarfare:t_90a",
      "uuid": "yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy",
      "respawnDelay": 1200,
      "destroyedTick": 1234567
    }
  }
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `active` | boolean | 系统是否激活 |
| `vehicles` | object | 载具字典，键为 vehicleId |
| `status` | string | `"alive"` / `"respawning"` / `"dead"` |
| `team` | string | `"attack"` / `"defense"` |
| `vehicleType` | string | 实体类型 ID |
| `uuid` | string | 实体 UUID（O(1) 查找用） |
| `respawnDelay` | int | 重生延迟（tick） |
| `destroyedTick` | int | 被摧毁时的服务器 tick |

### 2.2 复活券系统

**存储键：** `team_revive`
**代码位置：** `server_scripts/team_revive/config.js`

```json
{
  "attack": {
    "current": 15,
    "max": 30
  },
  "defense": {
    "current": 20,
    "max": 30
  }
}
```

### 2.3 职业系统 — 第三方全局存储

**代码位置：** `server_scripts/profession/config/c_storage.js`

职业模块新增**可选的**第三方全局存储机制。默认关闭，通过指令启用。

**目录结构：**
```
data/kubejs/db/profession_playerdata_db/
├── config.json                         ← 开关状态（双保险）
└── playerdata/<uuid>.json              ← 玩家职业数据备份
```

**玩家数据文件内容：**
```json
{
  "profession": "assault",
  "mainWeapon": "ak47",
  "offhandWeapon": "glock17",
  "specialWeapon": "rpg7",
  "taczAttachments": "{\"ak47\":{\"scope\":\"tacz:scope_8x\"}}",
  "professionBackpack": "{\"assault\":{\"1\":{...}}}",
  "_savedAt": "2026-07-07T..."
}
```

**双保险机制：**

| 存储位置 | 存储内容 | 说明 |
|---------|---------|------|
| `server.persistentData.profession_useExternal` | 开关状态 | 运行时内存，服务端重启可能丢失 |
| `config.json` | 开关状态 | 文件备份，**权威源**，重启后覆盖到 server.persistentData |

**备份内容：** 职业、主武器、副武器、特殊武器、配件配置（taczAttachments）、背包配置（professionBackpack）共 6 个字段。

**触发时机：**

1. **定时自动** — 启用后每 5 分钟全量备份所有在线玩家
2. **登录恢复** — 玩家登录时检测外部文件，不一致则覆盖
3. **手动触发** — `/profession backup` / `/profession restore`
4. **服务端启动** — 自动从 config.json 恢复开关状态

## 三、读写接口

### 读取

```javascript
function getStore(server) {
  let raw = server.persistentData.getString('存储键')
  if (!raw || raw === '') return {}  // 默认空值
  try { return JSON.parse(raw) } catch(e) { return {} }
}
```

### 写入

```javascript
function saveStore(server, data) {
  server.persistentData.putString('存储键', JSON.stringify(data))
}
```

## 四、设计原则

1. **最小化存储** — 只存储当前运行时状态（如载具存活/重生状态），不存储配置（配置在 `config.js` 中用 `const` 定义）
2. **JSON 序列化** — 所有持久数据以 JSON 字符串形式读写，确保可读性和跨版本兼容性
3. **容错处理** — 读取时检测空值和 JSON 解析异常，自动返回默认空对象
4. **模块隔离** — 每个模块使用独立的存储键，互不干扰

### 2.4 出生点选择器

**代码位置：** `server_scripts/spawn_compass/config.js`

| 存储位置 | 键名 | 类型 | 说明 |
|---------|------|------|------|
| `server.persistentData` | `spawn_visibility` | JSON string | 可见性映射 `{ pointId: [team1, team2] }` |
| `player.persistentData` | `spawn_selected` | string | 玩家已选的出生点 ID |
| `player.persistentData` | `spawn_allow_respawn` | boolean | 是否允许他人复活到自己 |

> `spawn_visibility` 会在模块停用时自动重置为 `{}`，`player.persistentData` 只清在线玩家。

## 五、注意事项

- ❌ **不要**使用 `/data merge storage minecraft:persistent` 来读写本系统的数据
- ❌ **不要**直接修改 `persistent_data.nbt` 文件（会造成数据损坏）
- ✅ 通过 KubeJS 的 `server.persistentData` API 读写
- ✅ 如需调试，可用 `/kubejs hand server.persistentData` 查看（如果装了相关调试工具）
