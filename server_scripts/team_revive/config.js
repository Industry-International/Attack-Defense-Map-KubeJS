// ============================================================
// 队伍复活券系统 - 配置文件
//
// 复活券模型（参考三角洲攻防模式）：
//   - 每个队伍拥有一个可消耗的复活券池
//   - 玩家死亡时消耗 1 张复活券
//   - 券归零时触发淘汰函数（只触发一次）
//   - 可通过命令/数据包为队伍增加复活券（不可超过上限）
//   - 未在 teams 中配置的队伍不使用复活券系统
// ============================================================

const TEAM_REVIVE_CONFIG = {
  // ======== 复活券队伍配置 ========

  /**
   * 使用复活券系统的队伍配置
   * 键：队伍名（小写）
   * 值：{ max: 最大复活券数, initial: 开局复活券数 }
   *
   * 未列出的队伍不使用复活券系统，其死亡不会被统计。
   */
  teams: {
    attacker: { max: 200, initial: 200 }
  },

  /** 是否在队伍成员死亡时向全队广播当前剩余复活券数 */
  broadcastDeathCount: true,

  // ======== 淘汰函数配置 ========

  /**
   * 进攻方被淘汰时调用的数据包函数路径（固定值）
   */
  functionPath: "game:eliminated",

  // ======== 计分板状态检测 ========

  /** 用于检测游戏状态的虚拟玩家名 */
  scoreHolder: 'state',
  /** 游戏状态计分板目标名 */
  scoreObjective: 'game_state',
  /** 表示"游戏进行中"的分数值 */
  activeValue: 1,

  // ======== 持久化数据 ========

  /** KubeJS persistentData 中使用的根键名 */
  persistKey: 'team_revive',

  /** 淘汰标记键名（防重复触发） */
  eliminatedKey: 'team_revive_eliminated',
}
